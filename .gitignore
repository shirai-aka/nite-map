.DS_Store
node_modules/
.claude/settings.local.json

# APIキー。絶対にコミットしない(雛形は .env.example)
.env

# build.js の生成物(data/books/*.yaml が原本)
prototype/books.json

# build.js が .env から書き出す。APIキーを含むのでコミットしない
prototype/config.js
