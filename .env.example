# このファイルを .env という名前でコピーし、値を書き込んで使う。
# .env は .gitignore 済みなのでコミットされない。
#
#   cp .env.example .env
#   (.env を編集)
#   node --env-file=.env build.js

# 楽天ウェブサービス(https://webservice.rakuten.co.jp/)で発行
RAKUTEN_APP_ID=
RAKUTEN_ACCESS_KEY=
RAKUTEN_AFFILIATE_ID=

# Amazonアソシエイトのタグ(例: xxxx-22)。無くてもビルドは通る
AMAZON_TAG=
